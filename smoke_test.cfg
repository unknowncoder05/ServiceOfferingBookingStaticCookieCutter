# smoke_test.cfg — BaseEphemeralCookieCutter overrides for smoke_test.sh
# This file is sourced as bash before the test runs.

COMPOSE_FILE="BackEndApi/local.yml"
DJANGO_SERVICE="django"
CELERY_SERVICE="celeryworker"
HEALTH_URL="http://localhost:8000/api/v1/health/"

# Point at the smoke-test env overrides (disables services that need real credentials)
EXTRA_ENV_OVERRIDES="smoke_test_envs/.django.override"
SYNC_PATHS="BackEndApi/src BackEndApi/requirements BackEndApi/compose"

